# Addressing the Editor's Comments on PEARL — Suggested Revisions

**Context.** The editor raised two rejection-driving issues for *Journal of Cheminformatics*:

1. PEARL benchmarks on "old" datasets (BACE, BBBP, ClinTox from MoleculeNet, plus FART/Flavor) when much higher-quality benchmark resources now exist through the **OpenADMET** initiative.
2. PEARL only positions CLMs (ChemBERTa, MolFormer, Uni-Mol) against each other and cites GNN results *from other papers* rather than running graph-based models (GCN/GIN/D-MPNN/etc.) in-house under the same protocol — so the claim "CLMs are competitive/best" is not actually substantiated by a controlled benchmark.

This document lays out concrete, scoped changes to the repo and manuscript that address both points, plus novel extensions that turn the revision into a stronger contribution rather than a defensive patch.

---

## EXECUTION PROGRESS LOG (resume point — read this first)

**If this session was interrupted, start here.** This section is a live, authoritative record of what has actually been done, what decisions were made, and exactly what to run next. Do not re-derive scope from the sections below without checking here first — those sections are the original proposal; this log is what's actually been executed against it, including scope changes made after discussion with the user.

### Confirmed scope decisions (from user, do not re-ask)

1. **New-dataset pipeline scope: FULL LADDER.** For each new dataset, reproduce all 4 PEARL modes (E2E LoRA across ChemBERTa-MLM, ChemBERTa-MTR, MolFormer, Uni-Mol x FL/WL losses; FT Embed; FT Embed+PC; RAFE with a fresh ZINC-250k FAISS index per dataset) plus Chemprop and a plain GCN/GIN baseline. This is the expensive option — expect this to take a long time; do not silently descope to something lighter without asking.
2. **New datasets (4, from TDC ADMET Group, OpenADMET-adjacent lineage):**
   - `herg` = hERG_Karim (TDC name), binary classification, cardiotoxicity/binding-affinity-like (BACE analogue)
   - `dili` = DILI (TDC name), binary classification, liver toxicity (ClinTox analogue)
   - `caco2` = Caco2_Wang (TDC name), regression, cell permeability, label already log-scale
   - `half_life` = Half_Life_Obach (TDC name), regression, PK half-life in hours, **heavily right-skewed → outlier-filtered** (see below)
3. **Reporting filter rule:** a new dataset is only written up in the manuscript revision if `best(CLM pipeline results, Chemprop/GCN results) > PC-only baseline` on that dataset. Apply this at the very end (Phase 9), after all results are in — do not pre-filter or skip running a dataset because it looks like it might not clear the bar.
4. **Environment policy:** keep using the single shared `effichem` micromamba env (user's explicit choice over creating an isolated env) — but pin every new package install and verify `transformers`/`peft`/`Trainer` still import afterward. Use `pip-constraints-effichem.txt` (repo root) with `pip install -c pip-constraints-effichem.txt <pkg>` for anything touching the ML stack.
5. **Large-file convention:** any artifact >50MB (model checkpoints, embedding matrices, FAISS indices) goes under `$PEARL_EXTRAS` (default `/export/cse/rmall/Raghvendra/EffiChem_Extras`), keeping the same path-pattern the rest of the repo already uses (see README "Large Files" section) — not committed to the repo, not left under `data/`.
6. This `editor_response_suggestions.md` file itself is an internal working doc — **do not reference it from README.md or any other file destined for the public GitHub repo.**

### Environment incident log (read before touching the `effichem` env again)

- `pip install PyTDC` (unconstrained) silently downgraded `transformers` 4.57.3→4.50.3, `accelerate` 1.12.0→0.33.0, `datasets`, `evaluate`, `tokenizers` — breaking every existing LoRA finetuning script's `Trainer`/`peft` import. **Fixed** by reinstalling pinned versions with `--no-deps`. A separate, likely-pre-existing `torch`(2.5.0+cu124)/`torchvision`(0.21.0+cu124) mismatch ("`operator torchvision::nms does not exist`") was also hit and fixed by pinning `torchvision==0.20.0+cu124` (the version that actually matches torch 2.5.0).
- Current known-good pinned versions (also in `pip-constraints-effichem.txt`): `transformers==4.57.3`, `accelerate==1.12.0`, `datasets==4.4.2`, `evaluate==0.4.6`, `tokenizers==0.22.2`, `torch==2.5.0`, `torchvision==0.20.0+cu124`, `peft==0.18.1`. **Before installing anything new in `effichem`, snapshot these with `pip freeze | grep -Ei "^(transformers|accelerate|datasets|evaluate|tokenizers|torch|torchvision|peft)=="` and re-check after.**
- **Never `exec_module` / directly execute a repo script to "test" it** — `finetune_bace_fl.py` has no `__main__` guard around its WandB sweep logic, and doing this launched 3 real training runs on the user's live WandB account (sweep `tdxv0vo6`, project `raghvendra5688/huggingface`). They were deleted via `wandb.Api()` afterward. Use `ast.parse()` for a safe syntax-only check instead.
- **Second, sneakier regression from the same `pip install PyTDC`:** it also silently downgraded `rdkit` 2025.09.4 → 2023.9.6. This didn't break any import (so the first round of "does it still import" checks missed it) — it silently changed which RDKit descriptors exist. `Descriptors.descList` has 217 entries on 2025.09.4 vs 210 on 2023.9.6; the 6 missing ones (`NumAtomStereoCenters`, `NumUnspecifiedAtomStereoCenters`, `NumSpiroAtoms`, `NumAmideBonds`, `NumHeterocycles`, `NumBridgeheadAtoms`) meant any PC-feature computation done with the degraded rdkit produced **467-dim vectors instead of 473**. Caught by comparing `pc_only_features/{ds}/train_pc_features.csv` column counts across datasets (bace=473 vs herg=467) — **this class of bug does not crash, it silently changes data shape**, so version drift on any package that affects computed *values* (not just importability) needs an explicit before/after check, not just "does it still import." Fixed by `pip install --no-deps rdkit==2025.9.4`; now pinned in `pip-constraints-effichem.txt`. **Any `pc_only_features/*` or other RDKit-descriptor-derived cache written between the PyTDC install and this fix is suspect** — check column count against a known-good dataset (e.g. `bace`, 473 cols) before trusting any cache file; `herg` and `half_life` caches were deleted and regenerated after the fix, `dili`/`caco2` were unaffected because they reused caches computed earlier in the session (before the downgrade).
- **Lesson for future package installs in this env:** a clean import is not sufficient evidence nothing changed. After any `pip install` touching the shared stack, diff `pip freeze | grep -Ei "^(transformers|accelerate|datasets|evaluate|tokenizers|torch|torchvision|peft|rdkit|xgboost|lightgbm|catboost|optuna|scikit-learn|numpy|scipy|pandas)=="` against the known-good list in `pip-constraints-effichem.txt`, and where feasible, re-derive one concrete data artifact (e.g. a PC feature vector's column count/shape) to confirm computed values, not just imports, are unchanged.
- The `effichem` env needs `export LD_LIBRARY_PATH="$MAMBA_ROOT_PREFIX/envs/effichem/lib:$LD_LIBRARY_PATH"` or `matplotlib` fails (`CXXABI_1.3.15 not found`) — the env ships its own newer `libstdc++` than the system one. Always set this after `micromamba activate effichem`.
- Standard activation snippet used throughout (put at the top of any interactive shell command that needs the env):
  ```bash
  export MAMBA_EXE='/export/home/rmall/.local/bin/micromamba'
  export MAMBA_ROOT_PREFIX='/export/home/rmall/micromamba'
  eval "$("$MAMBA_EXE" shell hook --shell bash --root-prefix "$MAMBA_ROOT_PREFIX")"
  micromamba activate effichem
  export LD_LIBRARY_PATH="$MAMBA_ROOT_PREFIX/envs/effichem/lib:$LD_LIBRARY_PATH"
  ```

### Phase status

- [x] **Phase 0 — PC-only baseline on the original 4 datasets (BACE/BBBP/ClinTox/Flavor).** Script: `scripts/smiles/ml/pc_only_modelling.py`. Results: `results/pc_only/pc_only_summary.csv`. Finding: PC-only ties or beats PEARL's own best CLM-based numbers on ClinTox (0.850 vs 0.850 FT Embed, vs 0.786 E2E LoRA) and Flavor (0.808 vs paper's 0.804 RAFE / 0.801 E2E LoRA); trails clearly only on BACE (0.498 vs 0.623) — the geometry-critical task. BBBP is in between (0.759 vs 0.798).
- [x] **Phase 1 — TDC dataset curation.** Script: `scripts/data_curation/tdc_data_curation.py` (supports `--dataset {herg,dili,caco2,half_life,all}`). SLURM: `bash/tdc/run_tdc_data_curation.sh`. Output: `data/clean/{herg,dili,caco2,half_life}_datasets/{train,valid,test}_clean.csv` + manifest at `data/clean/tdc_dataset_manifest.json` (task type, label col, split sizes per dataset). `half_life` uses IQR-in-log1p-space outlier removal (bounds fit on train only, applied to all 3 splits) — see `outlier_removal: "iqr_log1p"` in its `DATASET_CONFIG` entry; removed 7/4/2 rows from train/valid/test, mean dropped 19h→9.6h, max 1200h→99h. **Rerunning curation for a single dataset is safe and idempotent** (`--dataset half_life`); it merges into the existing manifest rather than overwriting other datasets' entries.
- [~] **Phase 2 — PC-only baseline extended to the 4 new datasets (regression support added).** Script: same `pc_only_modelling.py`, now handles `task: "regression"` (XGBRegressor/LGBMRegressor/CatBoostRegressor, Optuna maximizes `-RMSE` via 5-fold `KFold`, metrics = RMSE/MAE/R2/Spearman, optional `target_transform: "log1p"` applied to the training target only — `y_test` and reported metrics stay on the original scale) alongside the existing binary/multiclass path. Per-dataset SLURM scripts: `bash/tdc/run_{herg,dili,caco2,half_life}_pc_only.sh`.
  - **`dili` — DONE.** Best: CatBoost, MCC=0.765, AUC=0.914, Acc=0.875.
  - **`caco2` — DONE.** Best: CatBoost, R2=0.738, Spearman=0.845, RMSE=0.351.
  - **`herg` — RERUNNING** (killed once mid-run after the rdkit-downgrade discovery below; relaunched at PID 4054337 with fixed rdkit and cleared feature cache). 13,445 molecules to featurize, expect this to take a while (~10-15 min featurization alone based on flavor's similar size earlier in the session, then 20-trial Optuna x 3 models).
  - **`half_life` — RERUNNING** (relaunched at PID 4054338 with fixed rdkit + cleared feature cache; this is its second restart — first was for outlier removal, second was for the rdkit fix below).
  - **KNOWN BUG, FIXED IN CODE:** running separate `--dataset X` processes in parallel means each one's `main()` used to overwrite (not merge into) `results/pc_only/pc_only_summary.csv` — whichever finished last would clobber the other datasets' rows. **Fixed** in `pc_only_modelling.py::main()` to merge with the existing CSV by dataset key (already in effect for the `herg`/`half_life` reruns launched after the fix). `dili` and `caco2` finished under the OLD pre-fix code, each overwriting the summary CSV with just their own dataset — so **`results/pc_only/pc_only_summary.csv` may currently be missing rows**; rebuild it once all 8 datasets (4 original + 4 new) are done by reading every `results/pc_only/*_PC_Only_Results/metrics/{XGBoost,LightGBM,CatBoost}_metrics.json` directly rather than trusting the CSV in isolation.
  - **SECOND BUG FOUND AND FIXED: silent RDKit downgrade** (see incident log above) meant the *first* `herg`/`half_life` attempts computed 467-dim (not 473-dim) PC features with no error — caught by comparing column counts across datasets, not by any exception. Both were killed, RDKit was restored to 2025.9.4, their stale feature caches at `data/pc_only_features/{herg,half_life}/` were deleted, and both were relaunched fresh. **Before trusting any `herg`/`half_life` result, verify `data/pc_only_features/{herg,half_life}/train_pc_features.csv` has 473 (not 467) feature columns** (`pd.read_csv(...).shape[1] - 1` since the label column is included).
  - **Next action:** poll `ps aux | grep pc_only_modelling`; once `herg` and `half_life` finish, verify their feature cache column counts (473, per above), manually reconstruct the merged `pc_only_summary.csv` from all 8 datasets' per-dataset metrics JSON files, sanity-check `half_life` specifically improved post-outlier-removal (pre-removal smoke test was R2~0.03, Spearman~0.46-0.58 on unfiltered data — expect improvement now), then mark Phase 2 complete. Also add the two missing `bash/tdc/run_{herg,half_life}_pc_only.sh`-style SLURM scripts to the log as done (they already exist, written alongside `dili`/`caco2`'s).

- [ ] **Phase 3 — Chemprop + PyG GCN/GIN baselines, all 8 datasets.** Not started. Chemprop 1.6.1 and PyTorch Geometric 2.8.0 are already installed and verified working in `effichem` (PyG's `GCNConv` works despite `torch-scatter`/`torch-sparse` binary import warnings — PyG has a native fallback). 2x Tesla P100 16GB GPUs available. Need: a training script per baseline (likely `scripts/gnn/chemprop_baseline.py` and `scripts/gnn/pyg_gcn_baseline.py`, or a combined `scripts/gnn/` directory mirroring `scripts/smiles/ml/` conventions), handling both classification (bace/bbbp/clintox/flavor/herg/dili) and regression (caco2/half_life) task types, run on the *original* 4 datasets too (not just the new 4) since the editor's core complaint is that GNN baselines were only ever cited from other papers, never run in-house on PEARL's own splits. Per-dataset SLURM scripts needed for all 8 (16 total: 8 x Chemprop + 8 x GCN), GPU partition (mirror `gpu-H200 --gres=gpu:1` convention from existing finetuning scripts).
- [ ] **Phase 4 — folded into Phase 3 above** (GCN/GIN is being built alongside Chemprop, not as a separate later phase — keeping this line only so phase numbering matches earlier discussion in this file; treat Phase 3 and 4 as one combined unit of work).
- [ ] **Phase 5 — Regression-capable LoRA E2E finetuning trainer.** Not started. The existing `scripts/smiles/finetuning/finetune_*_{fl,wl}.py` and `scripts/unimol/finetuning/finetune_unimol_*_{fl,wl}.py` scripts are classification-only (WL/FL losses, MCC-based early stopping, sigmoid/softmax heads). Need a regression variant (MSE or Huber loss, R2/Spearman-based early stopping, linear head, no WL/FL distinction — or keep as a no-op single "MSE" loss option) for `caco2` and `half_life`. This is the most invasive code change remaining — study the existing trainer's custom-loss-injection mechanism (HuggingFace `Trainer` subclass) before touching it.
- [ ] **Phase 6 — Run E2E LoRA finetuning (3 CLM families x 2 losses where applicable) on the 4 new datasets.** Not started. Needs Phase 5 for caco2/half_life; herg/dili can reuse the existing classification trainer scripts directly (just new dataset paths). Per-dataset, per-model-family, per-loss SLURM scripts, mirroring `bash/smiles/run_bace_finetuning_fl.sh` / `bash/unimol/run_unimol_bace_fl.sh` naming and SBATCH conventions (`gpu-H200`, `--gres=gpu:1`, `-A H200`, `-q h200_qos`).
- [ ] **Phase 7 — FT Embed + FT Embed+PC modelling on the new datasets.** Not started. Needs Phase 6's finetuned embeddings first. Mirrors `scripts/smiles/ml/{dataset}_modelling_refactored.py` / `{dataset}_pc_modelling_refactored.py`, extended for regression task type (reuse the regression branch already built into `pc_only_modelling.py` as a reference implementation).
- [ ] **Phase 8 — RAFE pipeline for the new datasets.** Not started. Needs Phase 6/7. Re-embed ZINC-250k with each new-dataset-finetuned CLM variant, build new FAISS indices, extract RAFE features, run RAFE modelling (classification + regression). Mirrors `scripts/smiles/rag/*.py`.
- [ ] **Phase 9 — Aggregate everything, apply the reporting filter.** Not started. For each of the 4 new datasets, compare `best(all CLM-pipeline results, Chemprop, GCN)` vs `PC-only`; only datasets where the former wins get written into the manuscript revision. Produces the final evidence table for the editor response letter.

### If resuming with no memory of this session

1. Read this log top to bottom first.
2. Run `ps aux | grep -E "pc_only_modelling|chemprop|finetune_"` to see if anything is still running.
3. Check `results/pc_only/pc_only_summary.csv` and the per-dataset `results/pc_only/*/metrics/*.json` files to see how far Phase 2 actually got.
4. Re-verify the `effichem` environment is still healthy before running anything: activate it (snippet above) and run
   `python3 -c "from transformers import Trainer; from peft import LoraConfig; print('OK')"` — if this fails, fix it before doing anything else (see incident log above).
5. Continue from the first unchecked `[ ]` phase above.

---

## 1. The benchmark datasets are dated — bring in OpenADMET / TDC

### What's true about the current setup
Checked against `manuscript/PEARL_paper.tex` (§Materials and Methods) and `README.md`: PEARL trains on **BACE, BBBP, ClinTox** (MoleculeNet, scaffold split, DeepChem reference splits from 2018) and **Flavor/FART** (2024, but small and task-idiosyncratic). All four are classification tasks, three binary, one 5-class. There are no regression endpoints and no exposure/PK-relevant ADMET endpoints (clearance, half-life, CYP inhibition, hERG, solubility, etc.).

### Why the editor is right to push back
- MoleculeNet scaffold splits are known to contain label noise, near-duplicate leakage across splits, and are heavily saturated (GNN/CLM papers have reported AUC > 0.93 on BBBP since ~2020), so a new method "matching" these numbers is weak evidence of real advance.
- **OpenADMET** (UCSF / Octant / Open Molecular Software Foundation, ARPA-H + Gates Foundation funded) exists specifically to fix this: it produces open, assay-consistent, pre-competitive ADMET data and runs **blind prospective challenges** (e.g., the ExpansionRx–OpenADMET challenge, ~7,000 molecules from a real lead-optimization campaign against RNA-mediated disease targets) rather than static, possibly-leaky retrospective splits.
- The **TDC ADMET Benchmark Group** (Therapeutics Data Commons) is the community-standard modern alternative even short of a blind challenge: 22 curated endpoints (475–13,130 molecules each), standardized scaffold splits, and a public leaderboard, spanning absorption, distribution, metabolism, excretion, and toxicity — i.e., exactly the same property classes PEARL already targets (BBB permeability, toxicity) but with more endpoints and more rigorous curation.

### Concrete actions
1. **Add 3–5 TDC ADMET Group endpoints** that are direct analogues of what PEARL already models, so existing infrastructure (LoRA finetuning → FT Embed → FT Embed+PC → RAFE) transfers with minimal new code:
   - `BBB_Martins` (already conceptually covered — cross-check against current BBBP for consistency/leakage),
   - `hERG` or `hERG_Karim` (binding-affinity/binding-pocket task, directly comparable in spirit to BACE — a good second geometry-sensitive target for the Uni-Mol 3D story),
   - `AMES` or `DILI` (toxicity, analogous to ClinTox but larger and better curated),
   - `Caco2_Wang`, `Solubility_AqSolDB`, or `Half_Life_Obach` (regression — see point 2 below).
   TDC datasets are pip-installable (`pip install PyTDC`) and ship canonical scaffold splits, so `scripts/data_curation/` only needs a thin new loader, not a rewrite.
2. **Add at least one regression endpoint.** The manuscript itself lists "extending PEARL to regression tasks... with calibrated uncertainty" as unaddressed future work (Discussion). Doing this now, on a TDC regression endpoint, kills two birds: it answers the editor's benchmark-quality concern *and* pre-empts a "why classification only" review comment. RAFE's neighborhood features (logP/QED/SAS distributions, similarity-weighted means) are already real-valued and require no redesign for a regression head — only the tree-classifier objective needs to switch to Spearman/MAE-optimized regressors.
3. **Report a held-out, temporally- or scaffold-disjoint generalization check** using an OpenADMET blind-challenge-style split (even a self-constructed time/scaffold split on a TDC set works if the live challenge window has closed) to directly counter "these are old, saturated splits" — show PEARL/RAFE performance doesn't collapse under a harder, leakage-resistant split.
4. **Explicitly retain BACE/BBBP/ClinTox/Flavor** as a legacy comparison layer (for continuity with EffiChem and published GNN numbers) but reframe them in the manuscript as a validation set, with the new OpenADMET/TDC endpoints as the primary evidence base. Journal of Cheminformatics reviewers are likely to accept "old + new" more readily than a wholesale swap that discards prior work.

---

## 2. CLMs vs. everything else — the GNN comparison is not a real benchmark

### What's actually in the repo
`grep` across `scripts/` and `bash/` finds **no GCN/GNN/Chemprop/D-MPNN implementation anywhere in PEARL**. The "GNN baselines" in the manuscript (Table `tab:external_main`, Supplementary Table S5: Chemprop, GROVER-Large, AttentiveFP, Mole-BERT) are **numbers copied from the original publications**, not runs performed on the same data splits, seeds, hardware, or evaluation code. The manuscript is transparent about this (it flags a "split-difference caveat" for ClinTox in Supplementary §S5), which is honest — but it also means every "PEARL matches/beats GROVER" claim in the paper is a cross-paper comparison, not a controlled experiment. This is precisely the gap the editor is naming, and it's the harder of the two issues to fix, but also the highest-leverage one for the resubmission.

### Concrete actions
1. **Implement and run at least two GNN baselines in-house**, on the identical scaffold splits / train-val-test files already produced by `scripts/data_curation/`:
   - **Chemprop (D-MPNN)** — the de facto standard baseline, has a stable, well-documented Python package; minimal integration effort.
   - **GIN or GCN** (e.g., via PyTorch Geometric / DGL-LifeSci) — a plain, non-pretrained graph baseline. Include this *specifically* because reviewers increasingly ask "does a simple GCN close most of the gap?" — if it does, that's an important, publishable finding in itself, not a threat to the paper.
   Both should go through the *same* Optuna-tuned hyperparameter search budget, the *same* bootstrapped-CI evaluation protocol, and the *same* WL/FL class-imbalance handling already used for the tree classifiers, so the comparison is apples-to-apples rather than borrowed-numbers-to-apples.
2. **Add a "cheap baseline" tier that should have existed from the start.** PEARL already computes the 473-dim PC feature vector (RDKit descriptors + graph features + Morgan + MACCS) for every molecule (`scripts/smiles/ml/pc_feature_extraction_ft_model_refactored.py`). Training the existing Optuna-tuned XGBoost/LightGBM/CatBoost stage on **PC features alone, with no CLM embedding at all**, is a near-zero-cost ablation using code that already exists — it directly tests whether the CLM embedding is earning its keep at all versus classical descriptors + tree ensemble. If PC-only tree models are competitive with CLM-based ones on some datasets (plausible, given the manuscript's own finding that "adding PC features to finetuned embeddings provides minimal improvement"), that is an important and easy-to-report result.
3. **Report training/inference cost, not just parameter count.** PEARL's core efficiency argument is % trainable parameters reduced via LoRA. GNN baselines (especially plain GCN/GIN) are typically 10–100x cheaper to train from scratch than any LoRA-adapted 46M–100M-parameter CLM. A reviewer sympathetic to GNNs will ask "efficient relative to what — a needlessly large model class?" Reporting GPU-hours and wall-clock alongside parameter-percentage pre-empts that and lets PEARL make an honest "efficiency vs. accuracy" Pareto argument instead of a parameter-count argument alone.
4. **Reframe the contribution claim.** Instead of "CLMs (with LoRA) are the best models," which the current evidence doesn't support, position PEARL as **"a controlled framework for comparing representation classes (1D sequence, 2D graph, 3D geometry) under matched parameter-efficient adaptation and matched retrieval augmentation"** — with an honest result that whichever family wins is task-dependent (already true for CLM vs. Uni-Mol in the current results) and now extended to include graphs. This reframing is defensible with the data the authors will actually have after adding GNN baselines, whereas the current framing is not.

---

## 3. Novelty to add during revision (turns a defensive fix into a stronger paper)

These go beyond "satisfy the editor" and would make the resubmission read as an advance rather than a patch:

1. **A fourth representation track, not just a fourth baseline table.** Slot a graph model (Chemprop D-MPNN or GIN) into PEARL's existing four-mode ladder (E2E → FT Embed → FT Embed+PC → RAFE) exactly like Uni-Mol was. This requires: (a) LoRA-style parameter-efficient adaptation for the GNN (e.g., low-rank adapters on message-passing weight matrices, or adapter layers between MPNN blocks — there is emerging literature on PEFT for GNNs to draw on), and (b) RAFE feature extraction from GNN embeddings via the same FAISS/ZINC-250k pipeline. This directly extends the paper's central "parameter-efficient adaptation across representation classes" narrative from {1D, 3D} to {1D, 2D, 3D} and would make PEARL, to our knowledge, the first framework to apply matched PEFT + retrieval augmentation across all three molecular representation paradigms.
2. **RAFE as an applicability-domain / uncertainty signal.** RAFE already computes `zinc_nearest_sim` and neighborhood-density features as a byproduct of retrieval. Repurpose these explicitly as a confidence/OOD flag and validate them against the OpenADMET blind-challenge-style split from Section 1 — i.e., show that low ZINC-neighborhood similarity predicts higher prediction error. This is a natural, low-cost extension of existing RAFE features into an applicability-domain contribution, which is exactly the kind of practical, deployment-relevant result Journal of Cheminformatics favors.
3. **Extend RAFE to regression** (paired with the new TDC regression endpoint) by replacing the mean/std neighborhood statistics with the regression-target analogue (e.g., neighborhood mean/variance of the predicted property itself, computed leakage-free from ZINC-250k's own logP/QED/SAS, or from a second held-out property-labeled corpus). This operationalizes the "future work" the Discussion already promises, inside the same revision.
4. **Cross-representation retrieval.** Currently each CLM family gets its own FAISS index (ChemBERTa-indexed ZINC, MolFormer-indexed ZINC, Uni-Mol-indexed ZINC). An interesting ablation: does retrieving neighbors using a *different* model's embedding space (e.g., query with MolFormer, retrieve via the Uni-Mol 3D index) add complementary signal beyond same-space retrieval? This tests whether RAFE's value is representation-specific or general, and is a cheap experiment given the indices already exist for all three families.

---

## 4. Suggested order of operations

1. Add the PC-only tree-classifier baseline (§2.2) — trivial, uses existing code, immediately clarifies how much the CLM embedding is contributing.
2. Stand up Chemprop and one plain GNN (GIN/GCN) on the *existing* BACE/BBBP/ClinTox/Flavor splits first, to get an in-house-controlled version of Table `tab:external_main`/S5 (§2.1) — this is the single highest-priority fix since it's the crux of the editor's second objection.
3. Add 2–3 TDC ADMET Group endpoints (start with `hERG` and one toxicity + one regression endpoint) and rerun the full four-mode ladder plus the new GNN baselines on them (§1).
4. Only after 1–3 are in hand, decide whether to attempt the GNN-into-the-ladder extension (§3.1) and the OOD/applicability-domain analysis (§3.2) — these are higher-effort and are what would elevate the resubmission beyond "responded to reviewers" into a stronger paper, but 1–3 are what make the paper acceptable in the first place.

---

## References for the above (for the response letter / methods section)

- OpenADMET consortium (UCSF, Octant, Open Molecular Software Foundation; ARPA-H / Gates Foundation funded); ExpansionRx–OpenADMET blind challenge (Oct 2025), ~7,000-molecule real lead-optimization dataset across 9 ADMET endpoints. See `openadmet.org` and the associated Nature Communications paper on systematic open-science ADMET prediction ("Mapping the avoid-ome").
- Therapeutics Data Commons ADMET Benchmark Group: 22 endpoints, standardized scaffold splits, public leaderboard (`tdcommons.ai/benchmark/overview`).
- Chemprop / D-MPNN (Yang et al. 2019) — already cited in PEARL as a literature number; needs an in-house run.
- GIN (Xu et al. 2019) / GCN (Kipf & Welling 2017) as minimal, non-pretrained graph baselines.
- PEFT-for-GNN literature (adapters/low-rank updates on message-passing networks) as a starting point for the fourth-track extension in §3.1.
